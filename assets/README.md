# assets

Drop the marketplace card here before publishing:

- `card.png` — the template card image referenced by `--image` in `docs/PUBLISH.md`
  (use a **commit-pinned** raw GitHub URL when publishing).
- `hero.png` *(optional)* — the wide image at the top of `TEMPLATE.md`.
- `icon.png` *(optional)* — square icon.

Recommended card size: ~1200×630. You can generate one with the `image-generator` skill.
